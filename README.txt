To use the application, make sure Node is installed, in the base directory use this line:

ionic build

to build the app, then:

ionic serve -l

to see the app in ionic lab.
Or:

ionic cap add android
ionic cap open android

to see the app in android studio.